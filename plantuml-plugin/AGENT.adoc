= PlantUML Gradle Plugin — Directives Agent
:toc: left
:toclevels: 3
:icons: font
:sectnums:

[CAUTION]
====
**ARRET OBLIGATOIRE** avant `rm`, `Write` (ecrasement), ou suppression de fichier :
1. LIRE le fichier en entier
2. VERIFIER `git ls-files -- <fichier>` (si pas dans git = pas de retour possible)
3. DEMANDER confirmation fichier par fichier a l'utilisateur
4. ATTENDRE "oui" avant d'agir
====

== Avertissement — Racine de Travail

**Ce fichier est dans `plantuml-plugin/`**, la racine de travail du projet.

La racine du depot git (`plantuml-gradle/`) ne contient QUE le repo git et les fichiers
de build Gradle. TOUT le reste (code, tests, gouvernance, documentation agent) vit ici.

* Les chemins dans ce fichier sont relatifs a **`plantuml-plugin/`**
* L'agent travaille depuis **`plantuml-plugin/`**
* Les commandes Gradle s'executent depuis **`plantuml-plugin/`** ou avec `-p plantuml-plugin`

== Projet

**Nom** : plantuml-gradle (plugin Gradle pour generer des diagrammes PlantUML via LLM)
**Stack** : Kotlin, Gradle (plugin development), LangChain4j, Ollama/Gemini/OpenAI
**Documentation** : AsciiDoc + PlantUML + Gradle

== Regles Absolues

=== Git

**INTERDICTION FORMELLE** de commit/push/merge sans permission explicite de l'utilisateur.

Commandes autorisees (lecture seule) : `git status`, `git diff`, `git log`, `git show`
Commandes interdites (sauf ordre) : `git add`, `git commit`, `git push`, `git merge`, `git rebase`

=== Tests

L'agent NE DOIT JAMAIS lancer de tests (`./gradlew test`, etc.) lors de la procedure de fin de session SANS permission explicite de l'utilisateur.

=== Securite

* Jamais de secrets en dur dans le code
* Jamais de credentials dans les fichiers de config
* Jamais de tokens API commites dans Git
* Utiliser des references (`keyRef: ${GOOGLE_API_KEY_1}`)

=== Fichiers de configuration — REGLE ABSOLUE DE SURETE

**PRINCIPE** : Tout fichier de configuration (YAML, JSON, XML, TOML, properties, .env, etc.) peut contenir des donnees sensibles non versionnees. Modifier un tel fichier sans precaution met en danger l'integrite du travail du developpeur.

**INTERDICTIONS** :

* **NE JAMAIS ecraser** un fichier de configuration existant avec un `Write` complet si on peut faire un `Edit` partiel
* **NE JAMAIS remplacer** des valeurs sensibles existantes (tokens, mots de passe, cles API, URLs de production) par des valeurs factices ou placeholders
* **NE JAMAIS modifier** un fichier sans avoir **d'abord** verifie s'il est dans `.gitignore` (`git check-ignore <fichier>`) — un fichier ignore n'a pas de `git restore` possible

**Procedure OBLIGATOIRE** avant modification d'un fichier de configuration :

. `git check-ignore <fichier>` : Verifier si le fichier est versionne
. `git ls-files -- <fichier>` : Confirmer s'il est tracke
. Si le fichier **n'est pas tracke** (pas dans git) : **CREER UNE SAUVEGARDE** avant toute modification (`cp <fichier> <fichier>.bak`)
. Si le fichier contient des valeurs sensibles : **les PRESERVER intactes**, ne jamais les remplacer par des placeholders
. Privilegier **l'edit partiel** (modification ciblee) plutot que **l'ecriture complete** (ecrasement total)
. Apres modification : **verifier** que les valeurs sensibles sont toujours presentes

**Lecon de la Session 2 (bakery-plugin)** : Ecraser un site.yml non versionne avec des valeurs factices a detruit des tokens GitHub reels. Sans sauvegarde git (fichier ignore), la restauration aurait ete impossible sans une copie externe. Cela aurait bloque definitivement le developpeur si aucun fallback n'etait disponible. **Ce n'est pas une question de viser un fichier site.yml — c'est un comportement de surete qui ne met pas en danger le travail du developpeur.**

=== Fichiers personnels — Interdiction de modifier

_L'agent NE DOIT JAMAIS_ modifier, ecraser ou supprimer un fichier personnel de l'utilisateur (`~/.gitconfig`, `~/.bashrc`, `~/.ssh/config`, `~/.zshrc`, etc.) _SANS permission explicite de l'utilisateur_.

**PRINCIPE** : Les fichiers `~/.*` appartiennent a l'utilisateur. Leur modifier peut casser le workflow, les credentials, ou les preferences de l'environnement. L'environnement de travail de l'utilisateur est en dehors du scope du projet.

**INTERDICTIONS** :

* **NE JAMAIS** utiliser `Write` ou `Edit` sur un fichier `~/.*` ou `~/*` qui n'est pas dans le repertoire de travail du projet
* **NE JAMAIS** executer de commande touchant `~/.gitconfig`, `~/.bashrc`, `~/.ssh/*`, `~/.zshrc`, etc. meme avec l'intention de "reparer" ou "configurer"
* **Toujours rester dans le working directory** du projet actuel

_Lecon Session 033 (magic_stick)_ : L'agent a tente de modifier `/home/cheroliv/.gitconfig` pour changer le `user.name` de `opencode` vers `cheroliv`. Sans permission explicite. Modifier les configs utilisateur hors projet est un comportement catastrophique qui peut corrompre l'environnement de travail.

=== Methodologie de Travail

* **1 fichier a la fois** : Modifier un seul fichier avant de passer au suivant
* **Validation immediate** : Compiler apres chaque modification
* **Contexte leger** : Maximum 50k tokens EAGER (~3000 lignes)

== Architecture

=== Structure du Projet (depuis la racine de travail = ce repertoire)

[source,bash]
----
plantuml-plugin/                    # ⭐ RACINE DE TRAVAIL (ce repertoire)
├── src/main/kotlin/               # Code source Kotlin
├── src/test/                      # Tests unitaires + fonctionnels + Cucumber
├── .agents/                       # Documentation agent (sessions, archives, references)
│   ├── INDEX.adoc                 # Regles absolues + sessions recentes (EAGER)
│   ├── sessions/                  # Archives par session
│   └── archives/                  # Archives mensuelles
├── PROMPT_REPRISE.adoc            # Mission session en cours (EAGER)
├── BACKLOG.adoc                   # Suivi prioritaire des taches
├── TODO.adoc                      # Pense-bete informel
├── AGENT.adoc                     # Directives completes (ce fichier)
├── AGENT_MODUS_OPERANDI.adoc      # Methodologie LAZY/EAGER
└── build.gradle.kts               # Build du plugin
----

=== Contexte Git (pour reference)

[source,bash]
----
plantuml-gradle/                   # RACINE GIT (ne contient que le repo + bootstrap)
├── plantuml-plugin/               # ⭐ RACINE DE TRAVAIL
├── settings.gradle.kts            # Settings multi-projets
├── README_truth*.adoc             # Doc consommateur de plugin (pas notre gouvernance)
├── opencode.json                  # Configuration opencode → pointe vers AGENT.adoc
└── build.gradle.kts               # Build racine
----

=== Composants Cles

* **PlantumlPlugin** : Point d'entree du plugin Gradle
* **PlantumlManager** : Orchestrateur principal
* **LlmService** : Service d'appel aux LLM (Ollama, Gemini, OpenAI)
* **ApiKeyPool** : Pool de cles API avec rotation et quota tracking
* **ConfigLoader / ConfigMerger** : Chargement et fusion de configuration YAML/properties/env
* **ProcessPlantumlPromptsTask** : Tache Gradle de traitement des prompts
* **ValidatePlantumlSyntaxTask** : Tache Gradle de validation syntaxique
* **GenerateDiagramDocsTask** : Tache Gradle pour le dogfooding

=== Commandes Utiles

[source,bash]
----
# Depuis la racine de travail (plantuml-plugin/)
./gradlew compileKotlin

# Depuis la racine git (plantuml-gradle/)
./gradlew -p plantuml-plugin compileKotlin

# Tests unitaires
./gradlew test

# Tests fonctionnels
./gradlew functionalTest

# Tests Cucumber
./gradlew cucumberTest

# Tous les checks
./gradlew check

# Generer la documentation diagrammes
./gradlew generateDiagramDocs
----

== Gestion du Contexte — CRITIQUE

* **Que faire ensuite ?** → **`BACKLOG.adoc`** (suivi autoritaire et prioritaire des taches/bugs)
* **`TODO.adoc`** → pense-bete informel d'idees, PAS un plan, PAS une liste de priorites
* **`PROMPT_REPRISE.adoc`** → mission de la session en cours et priorites de la prochaine session

== Session Procedure

=== Debut de Session

1. Lire `PROMPT_REPRISE.adoc` pour la mission en cours
2. Lire `.agents/INDEX.adoc` pour les regles absolues
3. Charger les fichiers ESSENTIALS si l'EPIC est concernee

=== Fin de Session

**Mots-cles declencheurs** : "nouvelle session", "je quitte", "session terminee", "a plus tard", "on arrete la"

**Rotation backup** : Mots-cles explicites — "rotation backup", "backup rotation", "lance la rotation backup" — declenchent une rotation independante (hors fin de session).

**Procedure obligatoire en 6 etapes** :

1. **Creer l'archive LAZY** : `.agents/sessions/{N}-{type}-{sujet}.adoc`
2. **Mettre a jour PROMPT_REPRISE.adoc** — Mission N+1 (max 70 lignes)
3. **Mettre a jour SESSIONS_HISTORY.adoc** : `.agents/SESSIONS_HISTORY.adoc` — Ajouter la session terminee
4. **Mettre a jour INDEX.adoc** : `.agents/INDEX.adoc` — Date, session en cours N+1
5. **Mettre a jour TEST_COVERAGE_ANALYSIS.adoc** (si tests modifies)
6. **Mettre a jour COMPLETED_TASKS_ARCHIVE.adoc** : `.agents/archives/COMPLETED_TASKS_ARCHIVE_{mois}.adoc`

**Aucune etape ne peut etre marquee OK si le fichier n'a pas ete effectivement modifie et verifie.**

==== Capteur de Rotation Backup (etape 7 optionnelle)

**Declenchement automatique** — Apres l'etape 6, avant la checklist, l'agent verifie :

. `N % 10 == 0` (session multiple de 10)
. **OU** les fichiers EAGER cumules depassent **500 lignes** (`wc -l .agents/INDEX.adoc .agents/archives/COMPLETED_TASKS_ARCHIVE_*.adoc PROMPT_REPRISE.adoc`)

Si l'une des conditions est vraie, l'agent **execute l'etape 7** au sein de la procedure de fin de session :

. Creer le dossier `.agents/backup/Y{annee}-sessions-{plage}/` (plage = sessions N-5* et anterieures)
. Y copier **integralement** INDEX, SESSIONS_HISTORY, COMPLETED_TASKS_ARCHIVE et les fichiers sessions/ concernes
. Tronquer les fichiers actifs aux **10 dernieres sessions** (sliding window)
. Ajouter la localisation de la nouvelle vague dans INDEX.adoc (`_Localisation active_`)

La checklist s'affiche ensuite **apres** cette etape, avec `[✅] 7. Backup rote (si applicable)`.

== Anti-Patterns — Pieges a Eviter

|===
| # | Anti-Pattern | Symptome | Prevention
| 1 | **Agent sans `opencode.json`** | L'agent demarre nu, pose des questions basiques deja dans AGENT.adoc | Creer `opencode.json` AVANT `AGENT.adoc`, ne jamais l'oublier
| 2 | **Trop d'agents trop tot** | Agents stubs vides, l'agent hesite | Seuil : 100 lignes de doc metier ou 5 regles concretes avant de creer un agent specialise
| 3 | **PROMPT_REPRISE fantome** | La mission a derive, PROMPT_REPRISE ne reflete pas la realite | Mettre a jour systematiquement en fin de session, ne pas differer
| 4 | **Archive vide** | Template rempli de placeholders "TODO" -> "a completer plus tard" n'arrive jamais | Remplir integralement a chaque session, jamais de TODO dans les archives
| 5 | **Portefeuille fantome** | Nouveau projet cree mais pas ajoute aux INDEX.adoc des autres projets | Ajouter le projet dans TOUS les portefeuilles, pas seulement le sien
| 6 | **Redondance cachee** | Meme backlog copie-colle -> divergence entre AGENT.adoc et INDEX.adoc | INDEX.adoc = source de verite pour roadmap/EPICs. AGENT.adoc = storytelling uniquement
| 7 | **Session trop longue** | 2h, 85 messages, 12 fichiers -> LLM repete des erreurs deja corrigees | Splitter des que > 50 messages ou > 3 fichiers modifies. 5x20min > 1x2h
|===

=== Audit du Contexte

**Regle de vigilance** : Tout fichier EAGER > 100 lignes est un suspect. Il merite un audit. La question n'est pas "faut-il le supprimer ?" mais "faut-il le charger a chaque session ?"

**Procedure d'audit en 5 etapes** :

. **Mesurer** : `wc -l` sur tous les fichiers EAGER. Le total doit etre < 1000 lignes.
. **Identifier le suspect** : le fichier qui pese > 20% du total.
. **Auditer section par section** : Deja ailleurs ? → doublon. Jamais servi ? → mort. Information unique ? → critique.
. **Classifier** : doublons a retirer de l'EAGER, jamais utilise → cold storage, connaissance unique → a garder.
. **Splitter ou relocaliser** : unique et critique → ESSENTIALS compacte. Tout le reste → `encyclopedies/`.

**Suivi des metriques** : Tracer le total de lignes EAGER avant/apres chaque optimisation dans COMPLETED_TASKS_ARCHIVE.

=== Defense en Profondeur — Les 3 Couches

La gouvernance forme 3 couches de defense contre la saturation du contexte :

. **EAGER/LAZY** — structure par *disponibilite* (tableau de bord vs manuel du proprietaire)
. **Hot/Warm/Cold** — structure par *fraicheur* (10 sessions actives vs references vs backup)
. **ESSENTIALS/encyclopedie** — structure par *densite* (essentiel compact vs connaissance exhaustive)

=== Metaphore des 3 Etageres

* **Etagere 1 (EAGER)** — le plan de travail : INDEX recent, PROMPT_REPRISE, regles absolues. Leger, immediat.
* **Etagere 2 (LAZY)** — la bibliotheque de consultation : references techniques, procedures. Sur demande.
* **Cave (`backup/`)** — les archives froides : tout ce qui est passe. Jamais charge auto. Humain seulement.

=== Consultation du Backup (par l'humain)

Deux methodes pour retrouver une information archivee :

. **`grep` cible** : `grep -r "motif" backup/Y20XX-.../` — trouve tout, quel que soit l'angle (INDEX, SESSIONS_HISTORY, archive).
. **Reintegration manuelle** : copier temporairement le dossier de backup dans le contexte actif pour analyse par l'agent.

L'index du backup est **implicite** — il est dans la structure meme des fichiers archives (chaque fichier etant deja un index depuis son propre angle). Pas de `index-backup.adoc` central — ce serait une redondance de plus dans un systeme qui souffre deja de croissance lineaire.

=== Bootstrap d'un Nouveau Projet — 6 etapes

Pour initialiser la gouvernance agent sur un projet vierge :

. **opencode.json** — 6 lignes, `{ "instructions": ["AGENT.adoc"] }`. Toujours en premier.
. **Dossiers** — `mkdir -p .agents/sessions/ .agents/archives/ .agents/encyclopedies/`
. **AGENT.adoc** — fichier maitre (~200 lignes) : regles absolues, structure projet, conventions
. **PROMPT_REPRISE.adoc** — mission session 1 (max 70 lignes)
. **Fichiers LAZY structurants** : INDEX.adoc (EAGER dans .agents/), SESSIONS_HISTORY.adoc, TEST_COVERAGE_ANALYSIS.adoc (ou SCRIPT_VERIFICATION selon projet), LAZY_EAGER_ESSENTIALS.adoc
. **Portefeuille transverse** — ajouter le nouveau projet dans le tableau Portefeuille de TOUS les INDEX.adoc des autres projets

== Conventions de Code

* Kotlin idiomatic, pas de commentaires sauf demande
* Tests : JUnit5 + Cucumber (BDD)
* Configuration : YAML + properties + env vars
* Documentation : AsciiDoc (.adoc) uniquement

=== References de Style — Codebases LAZY

_Regle_ : Quand l'agent a besoin de reflechir au style de code, conventions Kotlin/Gradle, ou patterns de plugin,
il DOIT consulter ces codebases comme reference. Ces chemins sont LAZY — ne jamais charger leur contenu en EAGER.

|===
| Projet | Chemin | Role
| bakery-gradle | `/home/cheroliv/workspace/__repositories__/bakery-gradle/bakery-plugin` | Plugin Gradle convention-based
| plantuml-gradle | `/home/cheroliv/workspace/__repositories__/plantuml-gradle/plantuml-plugin` | Plugin Gradle mature, reference principale (ce projet)
| newpipe-gradle | `/home/cheroliv/workspace/__repositories__/newpipe-gradle` | Plugin Gradle
| slider-gradle | `/home/cheroliv/workspace/__repositories__/slider-gradle` | Plugin Gradle
| codebase-gradle | `/home/cheroliv/workspace/__repositories__/codebase-gradle` | Plugin Gradle
| readme-gradle | `/home/cheroliv/workspace/__repositories__/readme-gradle` | Plugin Gradle
| jhipster-gradle-plugins | `/home/cheroliv/workspace/__repositories__/jhipster-gradle-plugins` | Mono-repo persistence + assistant
|===

== Fichiers EAGER (charges automatiquement) — chemins relatifs a plantuml-plugin/

|===
| Fichier | Role

| `PROMPT_REPRISE.adoc` | Mission session en cours (max 70 lignes)
| `LAZY_EAGER_ESSENTIALS.adoc` | Principes LAZY/EAGER + politique de chargement
| `.agents/INDEX.adoc` | Regles absolues + sessions recentes
| `.agents/SESSIONS_HISTORY.adoc` | Tableau recapitulatif des sessions (max 10)
| `*_ESSENTIALS.adoc` | Contexte metier critique (si EPIC active)
| `AGENT.adoc` | Ce fichier — Directives completes
|===

=== Mécanisme de Backup — Hot/Warm/Cold Storage

* **Hot (EAGER)** : 10 dernières sessions dans INDEX, PROMPT_REPRISE N+1, sessions N-1 et N
* **Warm (LAZY)** : SESSIONS_HISTORY récent, références techniques, procédures, agents spécialisés
* **Cold (`backup/`)** : Sessions N-10 et antérieures — copie intégrale, sans transformation ni réindexation
* **Règle de rotation** : Tous les ~10 sessions, déplacer le paquet de fichiers agents (INDEX, SESSIONS_HISTORY, COMPLETED_TASKS_ARCHIVE, sessions/) tel quel dans `backup/`
* **Stockage** : `.agents/backup/Y{année}-sessions-{plage}/` — structure identique à `.agents/` actif
* **Principe** : Aucune donnée supprimée, tout relocalisé. L'agent ne charge jamais `backup/`. L'humain seul décide de la distillation.

== Fichiers LAZY (sur demande) — chemins relatifs a plantuml-plugin/

|===
| Fichier | Role

| `*_REFERENCE.adoc` | Architecture complete
| `.agents/sessions/{N}*.adoc` | Archives detaillees par session
| `.agents/archives/COMPLETED_TASKS_ARCHIVE_*.adoc` | Archive des taches terminees
| `SESSION_PROCEDURE.adoc` | Procedure de fin de session
| `AGENT_MODUS_OPERANDI.adoc` | Methodologie LAZY/EAGER
|===