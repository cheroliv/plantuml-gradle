= Test Coverage Analysis — PlantUML Gradle Plugin

_Dernière mise à jour_ : Session 134 (1er mai 2026)

=== Résumé Global

* **Total tests unitaires** : 380 PASSED, 0 FAILED
* **Tests fonctionnels GradleRunner KG** : 15 PASSED (nouveau session 128)
* **Tests Knowledge Graph** : 90 unitaires (Parser 26, Renderer 40, Task 15, Integration 9) + 15 fonctionnels

=== Knowledge Graph — Couverture par Composant

|===
| Composant | Tests | Scénarios couverts
| KnowledgeGraphParser | 26 | Legacy format, graphify natif (links/nodes/community int), flat format, edge strings, dedup, malformed JSON, fallbacks
| KnowledgeGraphRenderer | 40 | Empty graph, single/multi community, EXTRACTED/INFERRED/AMBIGUOUS, filtres combinés, unassigned nodes, custom colors, attribute limit, legend, cross-community edges, sanitize IDs, cleanLabel (dots/backticks/combined), isMethodNode (parens/dot/backtick/negative), nodeTypes filter (inclusion/exclusion/null), toString regression
| GenerateKnowledgeGraphDiagramTask | 15 | Task registration, missing graph.json, simple/rich diagrams, propriétés Gradle (edgeTypes, minConfidence, maxNodes, outputDir), filename convention, graphify natif, unknown type default
| GraphifyIntegrationTest | 9 | Full pipeline JSON→PNG, filtres (community/edgeType/minConfidence/maxNodes), nodes sans community, single node, large graph, parser→renderer cohérence
| KnowledgeGraphDiagramFunctionalTest | 15 | GradleRunner — graph.json absent, format graphify natif, labels propres (régression toString), confidence string, résolution ID→label, filtres (community/edgeTypes/maxNodes/minConfidence/nodeTypes/outputDir), PNG générée, graph vide, format legacy, JSON corrompu, filtre community inexistant
|===

=== Bugs découverts par les tests (Session 127-128)

1. **Renderer** : `$node.name` string interpolation → `${node.name}` (affichait toString()) — détecté visuellement, pas par les tests unitaires (code source correct, JAR mavenLocal périmé)
2. **Parser** : Résolution ID→label manquante (edges référençaient des IDs, pas des labels)
3. **Parser** : `confidence` asDouble() sur string retournait 0.0 (Jackson default, pas null)
4. **JAR mavenLocal** : Code source corrigé mais JAR non republié → consommateur utilisait ancienne version buggée

=== Couverture fonctionnelle (Session 128)

Les 15 tests fonctionnels GradleRunner couvrent les user journeys critiques :

| User Journey | Test | Statut
| graph.json absent → erreur claire | test 1 | ✅
| Format graphify natif | test 2 | ✅
| Labels propres sans toString() — régression | test 3 | ✅
| confidence=string (EXTRACTED/INFERRED) | test 4 | ✅
| Résolution IDs numériques → labels | test 5 | ✅
| Filtre communauté | test 6 | ✅
| Filtre edgeTypes | test 7 | ✅
| Filtre maxNodes | test 8 | ✅
| Filtre minConfidence | test 9 | ✅
| Output dir personnalisé | test 10 | ✅
| PNG générée quand PUML valide | test 11 | ✅
| Graph vide | test 12 | ✅
| Format legacy | test 13 | ✅
| JSON corrompu → échec | test 14 | ✅
| Filtre community inexistant → diagramme vide | test 15 | ✅

=== Lacunes identifiees (Session 130)

* ~~**Tests unitaires renderer** : `cleanLabel()`, `nodeTypes` filter, `isMethodNode()` non couverts~~ ✅ **COMBLÉS Session 130** — 15 tests ajoutés
* **Test de régression mavenLocal** : Aucun test ne vérifie que le JAR publié correspond au code source — c'est un problème de processus, pas de code